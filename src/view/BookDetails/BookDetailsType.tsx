export interface BookDetailsType {
  book?: {
    author?: string;
    description?: string;
    genre?: string;
    image?: string;
    status?: string;
    title?: string;
    userId?: string;
    id: string;
  };
}
